spring:
  datasource:
      url: jdbc:postgresql://localhost:5432/live-stream-recorder
      username: root
      password: 1234
      driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

#logging:
#  level:
#    com.emgc.livestreamrecorder: DEBUG